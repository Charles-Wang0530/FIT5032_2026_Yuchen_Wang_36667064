// Included for the Studio 12 requirement. This Vite project uses the matching
// `base` setting in vite.config.js when it builds for GitHub Pages.
module.exports = {
  publicPath:
    process.env.NODE_ENV === 'production' ? '/FIT5032_2026_Yuchen_Wang_36667064/' : '/'
}
